<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>PointNet++ 论文总结</title>
    <url>/2022/08/24/pointnet-plus-plus-lun-wen-zong-jie/</url>
    <content><![CDATA[<h2 id="第一遍阅读论文">第一遍阅读论文</h2>
<h3 id="感受">感受</h3>
<ol type="1">
<li><a
href="https://proceedings.neurips.cc/paper/2017/hash/d8bf84be3800d12f74d8b05e9b89836f-Abstract.html">PointNet++</a>
的网络结构和 FPN
结构很像，都使用“由上至下——下采样”、“横向连接——跨级链接”及“由下至上——上采样”的结构，从而对多尺度特征图进行融合，将高层的语义信息与低层的几何细节结合。</li>
<li>相比起 <a
href="https://openaccess.thecvf.com/content_cvpr_2017/html/Qi_PointNet_Deep_Learning_CVPR_2017_paper.html">PointNet</a>
论文，这篇论文更难懂一点。或许是因为 PointNet 结构很简单。</li>
</ol>
<h3 id="疑惑">疑惑</h3>
<ol type="1">
<li><p>PointNet++ 对点云进行多次降采样后调用 PointNet</p>
<p>那么调用的是 PointNet 的哪一个模型？是分类模型还是语义分割模型？
PointNet 的输出结果是怎样的？</p></li>
<li><p>采样层使用最远点采样法进行分组</p>
<p>计算距离的坐标基点是哪里？是原点吗？计算的是什么距离？是欧氏距离吗？</p></li>
</ol>
<h2 id="第二遍阅读论文">第二遍阅读论文</h2>
<h3 id="abstract">Abstract</h3>
<ol type="1">
<li><p>PointNet 的缺点</p>
<p>PointNet
不能很好地捕捉到局部结构信息，其识别细腻度模式的能力较弱，且难以应用在复杂场景</p></li>
<li><p>PointNet++ 的优点</p>
<p>PointNet++ 引入分层神经网络，在嵌套分区上递归应用 PointNet
；通过距离学习不同尺度的局部信息；加入集合学习层，以自适应地结合来自多个尺度的特征</p></li>
</ol>
<h3 id="introduction">Introduction</h3>
<ol type="1">
<li><p>PointNet++ 的特征提取过程</p>
<ol type="1">
<li>PointNet++ 通过底层空间的距离度量将点集划分为若干重叠的局部单元</li>
<li>PointNet++ 从局部单元中提取捕捉精细几何结构的局部特征；</li>
<li>PointNet++
将所有局部特征被进一步分组为更大的单元并被处理以产生更高层次的特征</li>
<li>PointNet++ 重复步骤 3 ，直到我们得到整个点集的特征。</li>
</ol></li>
<li><p>PointNet++ 面临的问题</p>
<ol type="1">
<li><p>如何产生局部单元</p>
<p>通过最远点采样法选取分区质心</p></li>
<li><p>如何通过局部特征学习器抽象出点集或局部特征</p></li>
<li><p>如何在密度不均匀的点集进行自适应分组</p>
<ul>
<li>在图像中，较小的卷积核可以提升 CNN 提取特征的能力</li>
<li>在点集中，在较小的邻域不足以让网络稳健地提取特征</li>
</ul></li>
</ol></li>
</ol>
<h3 id="problem-statement">Problem Statement</h3>
<p>设计一个输入为点集，输出为分级的语义信息的函数</p>
<h3 id="method">Method</h3>
<figure>
<img src="https://s2.loli.net/2022/08/19/xhqnjzpP1SsQKrR.png"
alt="PointNet++ 网络结构图" />
<figcaption aria-hidden="true">PointNet++ 网络结构图</figcaption>
</figure>
<ol type="1">
<li><p>回顾 PointNet</p>
<p><span class="math display">\[f(x_1,...,x_n) ≈
\gamma(\max_{i=1,...,n}\{h(x_i)\})\tag{1}\]</span></p>
<p>PointNet
简单高效，具有很强的鲁棒性，但是缺乏在不同尺度上捕捉局部结构的能力</p></li>
<li><p>分层特征学习</p>
<p>PointNet++ 的分层结构由多层 set abstraction 层级构成</p>
<p>每层 set abstraction 层级由采样层、分组层和 PointNet 层构成</p>
<ul>
<li><p>采样层从点集中选择一些点作为中心点</p></li>
<li><p>分组层寻找中心点周围的相邻点来构建局部单元</p></li>
<li><p>PointNet 层使用 mini-PointNet 从局部单元中提取特征</p></li>
</ul>
<p>set abstraction 层级的输入是 <span class="math inline">\(N \times (d
+ C)\)</span> 的矩阵，输出是 <span class="math inline">\(N&#39; \times
(d&#39; + C&#39;)\)</span> 的矩阵</p>
<blockquote>
<p>输入矩阵来自 $N $ 个具有 $ d $ 维坐标和 $ C$ 维特征的点 输出矩阵由
$N' $ 个具有 $ d' $ 维坐标和 $ C'$ 维特征的点组成</p>
</blockquote>
<ol type="1">
<li><p>采样层</p>
<p>该层的输入是点集 <span class="math inline">\(\{x_1, x_2, ..., x_n
\}\)</span> ，输出是子点集 <span class="math inline">\(\{x_{i_1},
x_{i_2}, ..., x_{i_n} \}\)</span></p>
<p>采样层使用最远采样法，从点集中选择几何距离较远的点作为子点集，子点集中的点即为中心点</p>
<p>常用的几何距离有欧氏距离和测地距离</p>
<p>选择初始点的方法有随机选取和选取距离点云重心的最远点</p>
<p>最远采样法相比于随机采样，能够更好覆盖整个点集；相比于 CNN
，最远采样法以一种依赖数据的方式生成感受野</p></li>
<li><p>分组层</p>
<p>该层的输入是整个点集构成的 <span class="math inline">\(N \times (d +
C)\)</span> 矩阵和采样层输出的子点集的坐标构成的 <span
class="math inline">\(N&#39; \times d\)</span> 矩阵，输出是 <span
class="math inline">\(N&#39; \times K \times (d + C)\)</span> 的矩阵</p>
<blockquote>
<p>K 是中心点附近的点数，可以自适应调整</p>
</blockquote>
<p>分组层使用球状半径查询，从点集中寻找中心点半径范围内的 K 个点</p>
<p>球状半径查询相比于 kNN 搜索，能够保证固定的区域尺度</p></li>
<li><p>PointNet 层</p>
<p>该层的输入是 <span class="math inline">\(N&#39; \times K \times (d +
C)\)</span> 矩阵，输出是 <span class="math inline">\(N&#39; \times (d +
C&#39;)\)</span> 的矩阵</p></li>
</ol></li>
<li><p>非均匀采样密度下的特征学习</p>
<ol type="1">
<li><p>多尺度分组</p>
<p>对每个尺度的分组都送入 PointNet
层进行特征提取，然后将特征连接起来作为中心点的特征</p>
<p>计算成本很高</p></li>
<li><p>多分辨率分组</p>
<p>将大尺度的特征和送入 PointNet 层提取的小尺度特征进行拼接</p>
<p>在密度较低的区域，自适应提升大尺度特征的权重</p>
<p>在密度较高的区域，自适应提升送入 PointNet
层提取的小尺度特征的权重</p></li>
<li><p>随机丢弃输入数据</p>
<p>在将数据输入进 PointNet 层之前进行 95% 的重新采样</p></li>
</ol></li>
<li><p>分割任务中点特征的传播</p>
<p>在语义分割任务中，希望得到所有原始点的点特征</p>
<ol type="1">
<li><p>将所有的点都作为中心点进行采样分组</p>
<p>计算成本很高</p></li>
<li><p>将特征用内插特征值从子采样点传播到原始点</p>
<p>内插特征值使用基于 k 近邻的反距离加权平均</p>
<p>将内插的特征值与跳过的链接点特征相连接</p>
<p><span class="math display">\[{f^{(i)}} = {\frac {\sum_{i=1}^{k}
{w_{i}(x)} {f}_{i}^{(j)}} {\sum_{i=1}^{k}{w_{i}(x)}}} \quad where \quad
{w}_{i}(x) = {\frac {1} {d(x, x_i)^p}}, j = 1, ..., C \qquad
\tag{2}\]</span></p></li>
</ol></li>
</ol>
<h3 id="experiments">Experiments</h3>
<ol type="1">
<li><p>数据集</p>
<ol type="1">
<li><p>MNIST</p>
<p>70k 张二维图片，按 6:1 比例划分训练集和测试集</p></li>
<li><p>ModelNet40</p>
<p>40 类 12311 个 CAD 模型，按 4:1 比例划分训练集和测试集</p></li>
<li><p>SHREC15</p>
<p>50 类 1200 个 CAD 模型，每个类别包括 24
种形状，使用五折交叉验证</p></li>
<li><p>ScanNet</p>
<p>1513 个室内扫描重建场景，按 4:1 比例划分训练集和测试集</p></li>
</ol></li>
<li><p>欧氏几何空间中的点集分类</p>
<p>在 MNIST 和 ModelNet40 数据集中的表现明显优于 PointNet</p>
<p>在模拟的不均匀数据集中，保持很好的性能</p></li>
<li><p>场景语义分割</p>
<p>对抽样密度变化具有稳健性</p></li>
<li><p>非欧几何空间中的点集分类</p>
<p>对每个模型构建由成对的测地线距离诱导的度量空间</p>
<p>提取 WKS 、 HKS
和多尺度高斯曲率等特征作为特征，而不是以三维坐标作为特征</p>
<p>因为以三维坐标作为特征不能揭示内在结构，而且受姿势变化的影响很大</p></li>
<li><p>特征可视化</p>
<p>通过特征可视化可以看出 PointNet++ 学习到了物体的基本结构</p></li>
</ol>
<h3 id="conclusion">conclusion</h3>
<p>改进方向：</p>
<p>如何通过在每个局部区域分享更多的计算来加快网络的推理速度</p>
<h2 id="复现结果">复现结果</h2>
<p>代码源自 yanx27 - <a
href="https://github.com/yanx27/Pointnet_Pointnet2_pytorch">PyTorch
Implementation of PointNet and PointNet++</a></p>
<p>输入如下指令开始训练 PointNet++ 分类模型</p>
<figure class="highlight py"><table><tr><td class="code"><pre><span class="line">python train_classification.py --model pointnet2_cls_ssg --log_dir pointnet2_cls_ssg</span><br></pre></td></tr></table></figure>
<p>训练最终结果</p>
<figure class="highlight py"><table><tr><td class="code"><pre><span class="line">Epoch <span class="number">200</span> (<span class="number">200</span>/<span class="number">200</span>):</span><br><span class="line">Train Instance Accuracy: <span class="number">0.976626</span></span><br><span class="line">Test Instance Accuracy: <span class="number">0.920146</span>, Class Accuracy: <span class="number">0.890121</span></span><br><span class="line">Best Instance Accuracy: <span class="number">0.926133</span>, Class Accuracy: <span class="number">0.898150</span></span><br><span class="line">End of training...</span><br></pre></td></tr></table></figure>
<p>输入如下指令开始测试 PointNet++ 分类模型</p>
<figure class="highlight py"><table><tr><td class="code"><pre><span class="line">python test_classification.py --log_dir pointnet2_cls_ssg</span><br></pre></td></tr></table></figure>
<p>测试最终结果</p>
<figure class="highlight py"><table><tr><td class="code"><pre><span class="line">Test Instance Accuracy: <span class="number">0.927751</span>, Class Accuracy: <span class="number">0.896012</span></span><br></pre></td></tr></table></figure>
<h2 id="参考文章">参考文章</h2>
<p>刘昕宸 - <a href="https://zhuanlan.zhihu.com/p/266324173">搞懂
PointNet++ ，这篇文章就够了！</a></p>
<p>Guoguang Du - <a
href="https://blog.csdn.net/dsoftware/article/details/107184116">最远点采样(Farthest
Point Sampling)介绍</a></p>
<h2 id="论文总结">论文总结</h2>
<h3 id="pointnet-针对-pointnet-提出的改进">PointNet++ 针对 PointNet
提出的改进</h3>
<ol type="1">
<li><p>PointNet
只是提取点云中各个点的空间特征，然后聚合成全局信息。这样不能很好地提取局部结构信息</p>
<p>PointNet++
通过采样和分组将点集划分为不同的重叠区域，即整合局部邻域</p></li>
<li><p>PointNet
在聚合成全局信息时只使用了最大值池化。这样会损失大量信息</p>
<p>PointNet++
采用多层神经网络，对点集中的分组不断进行下采样，提取不同尺度下的
local-global feature</p></li>
<li><p>PointNet
中的语义分割模型提取的中间特征是全局特征和局部特征拼接而成，特征的差异度不够</p>
<p>PointNet++
先进行下采样再进行上采样，使用跨级链接和内插特征值拼接对应层的
local-global feature</p></li>
</ol>
]]></content>
      <categories>
        <category>技术</category>
        <category>学习</category>
      </categories>
      <tags>
        <tag>PointNet++</tag>
        <tag>点云</tag>
      </tags>
  </entry>
  <entry>
    <title>PointNet 论文总结</title>
    <url>/2022/08/23/pointnet-lun-wen-zong-jie/</url>
    <content><![CDATA[<p>作者提出了使用对称网络提取点云特征、局部特征与全局特征相结合的点云处理方法</p>
<p>Point Net
网络简单高效，具有很强的鲁棒性，能够抵抗异常点插入、部分点缺失和部分点抖动</p>
<p>Point Net 网络由 MLP 、全连接层和最大池化层组成</p>
<h2 id="abstract">Abstract</h2>
<p>PointNet
是直接使用点云进行三维目标分类、语义分割、场景语义解析的一种语义感知网络模型</p>
<p>PointNet 网络简单高效，面对微小扰动、点云缺失有很强的鲁棒性</p>
<h2 id="introduction">Introduction</h2>
<ol type="1">
<li><p>卷积不适用于点云数据</p>
<p>因为卷积结构要求输入数据高度规整，而点云是无序、不规整的</p></li>
<li><p>点云不适宜转换为其他形式进行语义感知</p>
<p>因为点云转换为其他形式进行处理时，会使得数据变得庞大</p></li>
<li><p>PointNet
网络的输入是三维坐标集合，输出是每点的标签/物体的标签</p></li>
<li><p>PointNet
网络能够学会利用稀疏的关键点来概括物体的形状，以判断物体的类别</p></li>
</ol>
<h2 id="related-work">Related Work</h2>
<h3 id="点云特征">点云特征</h3>
<p>现有的点云特征提取是针对特殊任务定制的，而找到最佳特征组合并非易事</p>
<h3 id="三维数据的深度学习">三维数据的深度学习</h3>
<ol type="1">
<li><p><em>Volumetric CNNs</em></p>
<p>在体素上应用卷积神经网络</p>
<p>受限于计算成本高和点云稀疏导致转换后的体素分辨率低</p></li>
<li><p><em>FPNN</em> 和 <em>Vote3D</em></p>
<p>提出了处理点云稀疏问题的办法</p>
<p>难以处理数量较大的点云</p></li>
<li><p><em>Multiview CNNs</em></p>
<p>将三维点云转换为二维图像，然后使用 CNN 进行分类</p>
<p>不能胜任语义分割、点云补全等任务</p></li>
<li><p><em>Feature-based DNNs</em></p>
<p>将三维点云转换为矢量，通过提取图形特征来进行分类</p>
<p>受限于特征提取能力，不能很好的完成任务</p></li>
</ol>
<h3 id="无序数据的深度学习">无序数据的深度学习</h3>
<p>人们尚未探索基于无序点集的深度学习领域</p>
<p>而无序数据处理领域少数的硕果，也是关注于 NLP ，不适用于无序点集</p>
<h2 id="problem-statement">Problem Statement</h2>
<ol type="1">
<li><p>点云是三维坐标几何</p></li>
<li><p>对于物体分类任务，网络输出的结果应该是对 k 类的 k 个分数</p></li>
<li><p>对于语义分割任务，网络输出的结果应该是 n 个点对 m 类的 n * m
个分数</p></li>
</ol>
<h2 id="deep-learning-on-point-sets">Deep Learning on Point Sets</h2>
<h3 id="点集的特性">点集的特性</h3>
<ol type="1">
<li><p>无序性</p>
<p>点集是无序且不规整的，因此网络模型需要使用对称函数来提取特征</p>
<blockquote>
<p>对称函数：无论输入顺序如何变化，输出结果保持不变</p>
</blockquote></li>
<li><p>联系性</p>
<p>点集中的每个点不适孤立存在的，都是与周围的点联系的</p>
<p>因此网络模型要能够从附近点捕捉到局部结构，也能将局部特征和全局特征结合起来</p></li>
<li><p>变换不变性</p>
<p>点集在刚性变换后不会改变其语义标签</p>
<p>因此输入刚性变换后的点集，网络模型的输出应该保持不变</p>
<p>因此需要设计对准网络来提取点集特征</p></li>
</ol>
<h3 id="point-net-的结构">Point Net 的结构</h3>
<figure>
<img src="https://s2.loli.net/2022/08/09/PdzYgLJWnkHE4K3.png"
alt="Point Net 网络结构图" />
<figcaption aria-hidden="true">Point Net 网络结构图</figcaption>
</figure>
<p>Point Net 网络具有三大关键组成：</p>
<ul>
<li>使用最大池化层作为对称函数从点云中提取信息</li>
<li>局部特征与全局特征的结合机制</li>
<li>使用对准网络对齐输入点集及其特征</li>
</ul>
<ol type="1">
<li><p>针对无序输入的对称函数</p>
<p>对于无序序列，有以下三种解决方法：</p>
<ol type="1">
<li><p>按特定顺序对序列进行排序</p>
<p>但是在高维空间，很难找到一种稳定可靠的排列方式</p>
<p>而且实验证明排序对结果提升不大</p></li>
<li><p>进行数据增强后输入给 RNN</p>
<p>但是 RNN 难以处理大量数据</p></li>
<li><p>使用对称函数来提取点集中的特征</p>
<p>通过集合中的转换元素应用对称函数来近似定义在点集上的对称函数</p>
<p>Point Net 网络采用该种方法</p>
<p><span class="math display">\[f(\{x_1,...,x_n\}) ≈
g(h(x_1),...,h(x_n))\tag{1}\]</span></p>
<p><span class="math display">\[f:{ 2^{ \mathbb{R} } }^N \to \mathbb{R},
h:{ \mathbb{R} }^N \to { \mathbb{R} }^K, g:\underbrace{ {\mathbb{R} }^K
\times \cdots \times {\mathbb{R} }^K }_{n} \to \mathbb{R}\]</span></p>
<p>在具体实现中，使用 MLP 来近似 <em>h</em>
，用单变量函数和最大池化函数来近似 <em>g</em></p></li>
</ol></li>
<li><p>局部特征和全局特征相结合</p>
<p>对称函数输出的是描述输入点集全局特征的一个向量</p>
<p>要实现点云分割需要结合局部特征和全局特征</p>
<p>Point Net
网络将全局特征和每个点的特征连接起来再提取每个点新的特征</p></li>
<li><p>对准网络</p>
<p>对点云进行处理需要保证输入点集进行刚性变换后其语义标签不变</p>
<p>一种解决方案是在特征提取前将所有输入点集对齐到一个典型空间</p>
<p>Point Net 网络通过一个微型网络（ T-Net
）预测一个矩阵，将该矩阵与输入坐标相乘实现变换</p>
<p>T-Net
也可以应用在特征空间的对齐，但是由于维度大，矩阵不好优化，需要加以约束，使得矩阵接近正交矩阵</p>
<p><span class="math display">\[L_{reg} = {||I -
AA^T||}^{2}_{F}\tag{2}\]</span></p></li>
</ol>
<h3 id="理论分析">理论分析</h3>
<ol type="1">
<li><p>普遍近似</p>
<p>Point Net 网络具有对连续聚合函数普遍近似的能力，能够抵抗微小扰动</p>
<p>当最大池化层有足够多神经元，即式(1)中 K 足够大时， <em>f</em>
可以被任意近似</p>
<p>在最坏情况下， Point Net 网络
可以将空间划分为相同大小的体素，学习将点云转换为体积表示</p></li>
<li><p>瓶颈维度和稳定性</p>
<p>Point Net 网络的表现收到最大池化层维度的影响，即式(1)中 K</p>
<p>输入点集中的微小扰动不会影响 Point Net 网络的输出</p>
<p>Point Net
网络输出的结果实际上由点集中的部分点集决定，而该部分点集由式(1)中 K
决定</p>
<p>该部分点集 <span class="math inline">\(C_s\)</span>
被称为关键点集，式(1)中 K 被称为瓶颈维度，而最大可能的点集 <span
class="math inline">\(N_s\)</span> 被称为上限点集</p>
<p>关键点集实际上形成了一个物体的骨架</p></li>
</ol>
<h2 id="experiment">Experiment</h2>
<h3 id="应用">应用</h3>
<ol type="1">
<li><p>物体分类</p>
<p>数据集：ModelNet40</p>
<p>数据构成：12311 个 CAD 模型，40 类物体，4:1 划分训练集和测试集</p>
<p>评价指标：准确率</p>
<p>数据预处理：对网格进行均匀采样后转换为单一球体，即转换为点云</p>
<blockquote>
<p>实际项目中的数据预处理使用 RealityCapture 软件完成</p>
</blockquote>
<p>数据增强：沿上轴随机旋转后通过高斯噪声对每个点位置进行抖动</p>
<p>结果：速度很快，分类准确率很高，但略逊于基于多视图的 MV CNN</p>
<p>分析：网络只有全连接层和最大池化层， CAD
模型转换为点云时损失了几何细节</p></li>
<li><p>物体语义分割</p>
<ul>
<li><p>完整点云实验</p>
<p>数据集： ShapeNet part data set</p>
<p>数据构成：16881 个 CAD 模型，16 类物体，每个物体含有 2 ~ 5
个部件，共有50 类部件</p>
<p>评价指标： mIoU</p>
<p>结果：比大多数现有方法 mIoU 更高</p></li>
<li><p>不完整点云实验</p>
<p>数据集： ShapeNet part data set</p>
<p>数据处理：用 Blensor Kinect Simulator
从六个随机视点生成不完整的点云</p>
<p>结果：相较完整的点云，平均 IoU 只损失了 5.3%</p></li>
</ul></li>
<li><p>场景语义分割</p>
<p>数据集： Stanford 3D semantic parsing data set</p>
<p>数据构成：271 个房间扫描的点云，包含 13 类物体</p>
<p>特殊处理：将房间划分为 1 m * 1 m
的区块，训练时在每个区块随机抽取4096个点</p>
<p>结果：明显优于使用定制的点云特征提取方法</p></li>
<li><p>场景中物体检测</p>
<p>结果：明显优于基于滑动形状法的 SVM</p></li>
</ol>
<h3 id="结构设计分析">结构设计分析</h3>
<ol type="1">
<li><p>对称函数间的比较</p>
<p>对比最大池化、平均池化和基于注意力的加权和</p>
<blockquote>
<p>基于注意力的加权和：从每个点特征预测权值，归一化后计算与特征的加权和</p>
</blockquote>
<p>最大池化的效果最好</p></li>
<li><p>输入转换和特征转换的有效性</p>
<p>使用 T-Net 进行转换使得性能提升 0.8%</p></li>
<li><p>Point Net 网络的鲁棒性</p>
<p>当 50% 的点缺失时，准确率只下降约 3%</p>
<p>当 20% 的点是离群的异常点时，准确率仍高于 80%</p>
<p>当高斯噪声使得点云抖动时，准确率依旧保持得很好</p></li>
</ol>
<h3 id="point-net-网络结果可视化">Point Net 网络结果可视化</h3>
<p>关键点集 <span class="math inline">\(C_s\)</span>
概括了物体形状的骨架</p>
<h3 id="时空复杂度分析">时空复杂度分析</h3>
<p>Point Net 网络比 MVCNN 和 3D CNN 计算成本要低</p>
<p>Point Net 网络在 TensorFlow 上使用 1080X GPU 每秒可以处理 1000K
个点，完成 1K 个物体的分类或 2 个房间内的场景语义分割</p>
<h2 id="conclusion">Conclusion</h2>
<p>Point Net
网络直接使用点云完成物体分类、语义分割和场景语义解析，且获得了很好的结果</p>
<h2 id="补充">补充</h2>
<p>Point Net 模型的激活函数均使用 ReLu 函数</p>
<p>在进行点云分类时，在最后的 MLP 中加入了 Dropout 层</p>
<h2 id="问题与解答">问题与解答</h2>
<ol type="1">
<li><p>如何将全局特征和局部特征连接在一起？</p>
<p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">global_feat_expand = tf.tile(global_feat, [<span class="number">1</span>, num_point, <span class="number">1</span>, <span class="number">1</span>])</span><br><span class="line">concat_feat = tf.concat(<span class="number">3</span>, [point_feat, global_feat_expand])</span><br></pre></td></tr></table></figure></p>
<p>使用 <code>tf.tile(input, multiples, name=None)</code>
将全局特征复制拓展，使用
<code>tf.concat([tensor1, tensor2], axis)</code> 与局部特征拼接起来</p>
<p><code>tf.tile(input, multiples, name=None)</code>是将 input
各维度对应 multiples 参数复制</p>
<blockquote>
<p>multiples 参数维度必须 input 维度一致，表示在第几维上复制几次
<code>multiples = [1, num_point, 1, 1]</code> 表示在第二维上复制
num_point 次</p>
</blockquote>
<p><code>tf.concat([tensor1, tensor2], axis)</code>是将 tensor1 和
tensor2 在 axis 维度上拼接起来</p>
<table>
<thead>
<tr class="header">
<th>变量</th>
<th>Shape</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>point_feat</td>
<td>[None, num_point, 1, 64]</td>
</tr>
<tr class="even">
<td>global_feat</td>
<td>[None, 1, 1, 1024]</td>
</tr>
<tr class="odd">
<td>global_feat_expand</td>
<td>[None, num_point, 1, 1024]</td>
</tr>
<tr class="even">
<td>concat_feat</td>
<td>[None, num_point, 1, 1088]</td>
</tr>
</tbody>
</table></li>
<li><p>T-Net 网络内部是怎样的？</p>
<table>
<thead>
<tr class="header">
<th>处理</th>
<th>Shape</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Input</td>
<td>[None, num_point, 3, 1]</td>
</tr>
<tr class="even">
<td>1*3卷积核-64</td>
<td>[None, num_point, 1, 64]</td>
</tr>
<tr class="odd">
<td>conv1-128</td>
<td>[None, num_point, 1, 128]</td>
</tr>
<tr class="even">
<td>conv1-1024</td>
<td>[None, num_point, 1, 1024]</td>
</tr>
<tr class="odd">
<td>Max Pooling</td>
<td>[None, 1, 1, 1024]</td>
</tr>
<tr class="even">
<td>Reshape</td>
<td>[None, 1024]</td>
</tr>
<tr class="odd">
<td>FC-512</td>
<td>[None, 512]</td>
</tr>
<tr class="even">
<td>FC-256</td>
<td>[None, 256]</td>
</tr>
<tr class="odd">
<td>MLP</td>
<td>[None, 1, 9]</td>
</tr>
<tr class="even">
<td>Reshape</td>
<td>[None, 3, 3]</td>
</tr>
<tr class="odd">
<td>Output</td>
<td>[None, 3, 3]</td>
</tr>
</tbody>
</table>
<p>使用 T-Net
对数据进行转换可以理解为将数据进行矫正，对齐到到统一视角</p></li>
<li><p>为什么使用 Shared MLP ？</p>
<p>和普通 MLP 一样，都是实现特征提取</p>
<p>但是由于每个点的权重共享，可以节约大量参数</p></li>
<li><p>如何对点集应用 Shared MLP ？</p>
<p>实现时，使用 conv1，即使用 1 * 1 的卷积核进行卷积</p></li>
<li><p>模型的 Loss 如何计算？</p>
<p><code>classify_loss + mat_diff_loss * reg_weight</code></p>
<p>平均多分类稀疏 softmax 交叉熵 + T-Net 产生预测矩阵的约束（式(2)） *
权重（默认为0.001）</p></li>
</ol>
]]></content>
      <categories>
        <category>技术</category>
        <category>学习</category>
      </categories>
      <tags>
        <tag>点云</tag>
        <tag>PointNet</tag>
      </tags>
  </entry>
  <entry>
    <title>WSL 安装总结</title>
    <url>/2022/08/24/wsl-an-zhuang-zong-jie/</url>
    <content><![CDATA[<p>考虑到虚拟机不能使用 GPU 加速，现在打算配置 WSL 在 Ubuntu
上进行训练</p>
<h2 id="环境要求">环境要求</h2>
<p>Windows 10 版本 2004 及更高版本</p>
<h2 id="wsl-安装步骤">WSL 安装步骤</h2>
<ol type="1">
<li><p>启用系统功能</p>
<p>控制面板 =⇒ 程序 =⇒ 程序和功能 =⇒ 启用或关闭 windows
功能，勾选下列选项</p>
<ul>
<li>Hyper-V</li>
<li>适用于 Linux 的 Windows 子系统</li>
<li>虚拟机平台</li>
</ul>
<p>然后重新启动计算机</p></li>
<li><p>下载并安装<a
href="https://wslstorestorage.blob.core.windows.net/wslblob/wsl_update_x64.msi">适用于
x64 计算机的 WSL2 Linux 内核更新包</a></p></li>
<li><p>将 WSL 2 设置为默认版本</p>
<p>打开 PowerShell ，输入如下指令</p>
<p><figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line">wsl <span class="literal">--set-default-version</span> <span class="number">2</span></span><br></pre></td></tr></table></figure></p></li>
<li><p>安装 Linux 分发</p>
<p>在 PowerShell 中输入如下指令可以查看可获取的 Linux 分发版本</p>
<p><figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line">wsl <span class="literal">--list</span> <span class="literal">--online</span></span><br></pre></td></tr></table></figure></p>
<p>推荐选择 Ubuntu-20.04.3 LTS</p>
<p>输入如下指令安装 Linux 分发</p>
<p><figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line">wsl <span class="literal">--install</span> <span class="literal">-d</span> &lt;Distribution Name&gt;</span><br></pre></td></tr></table></figure></p>
<blockquote>
<p>WSL --install 默认安装固定版本的 Ubuntu -d 参数可以选择任意其他 Linux
分发版本 &lt;Distribution Name&gt; 替换为 Linux 分发版本的名称</p>
</blockquote></li>
<li><p>安装后设置用户账号和密码</p></li>
</ol>
<h3 id="wsl-安装参考网页">WSL 安装参考网页</h3>
<p>Microsoft - <a
href="https://docs.microsoft.com/zh-cn/windows/wsl/install-manual">旧版
WSL 的手动安装步骤</a></p>
<p>憨憨不敢_ - <a
href="https://blog.csdn.net/weixin_45191709/article/details/125871102">2022
window下安装ubuntu22.04（wsl升级 包含 podman &amp; docker ）</a></p>
<h2 id="wsl-配置">WSL 配置</h2>
<ol type="1">
<li><p>WSL2 连接 Windows 防火墙</p>
<ol type="1">
<li><p>检查 WSL 与 Windows 连接</p>
<p>在 PowerShell 中输入如下指令获取 Windows 本机 IP 和 WSL IP</p>
<p><figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line">ipconfig</span><br></pre></td></tr></table></figure></p>
<p>Windows 本机 IP 地址如下</p>
<p><figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line">无线局域网适配器 WLAN:</span><br><span class="line"></span><br><span class="line">    IPv4 地址 . . . . . . . . . . . . :</span><br></pre></td></tr></table></figure></p>
<p>WSL IP 地址如下</p>
<p><figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line">以太网适配器 vEthernet (WSL):</span><br><span class="line"></span><br><span class="line">    IPv4 地址 . . . . . . . . . . . . :</span><br></pre></td></tr></table></figure></p>
<p>分别在 PowerShell 和 WSL 中输入下列指令检查连接</p>
<p><figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line">ping &lt;IPv4 Address&gt;</span><br></pre></td></tr></table></figure></p>
<blockquote>
<p>Linux 系统中 Ping
指令不会自行停止，需要使用<code>Ctrl + C</code>停止指令</p>
</blockquote></li>
<li><p>若不能 ping 通，则检查 Windows 防火墙：</p>
<p>控制面板 =⇒ 系统和安全 =⇒ Windows Defender 防火墙 =⇒ 高级设置</p>
<ul>
<li>入站规则</li>
<li>出站规则</li>
</ul>
<p>启用其中所有 WSL 规则</p></li>
<li><p>若不存在该规则则需要自行创建：在 PowerShell
中以管理员身份输入如下指令</p>
<p><figure class="highlight powershell"><table><tr><td class="code"><pre><span class="line"><span class="built_in">New-NetFirewallRule</span> <span class="literal">-DisplayName</span> <span class="string">&quot;WSL&quot;</span> <span class="literal">-Direction</span> Inbound  <span class="literal">-InterfaceAlias</span> <span class="string">&quot;vEthernet (WSL)&quot;</span>  <span class="literal">-Action</span> Allow</span><br><span class="line"><span class="built_in">New-NetFirewallRule</span> <span class="literal">-DisplayName</span> <span class="string">&quot;WSL&quot;</span> <span class="literal">-Direction</span> Outbound  <span class="literal">-InterfaceAlias</span> <span class="string">&quot;vEthernet (WSL)&quot;</span>  <span class="literal">-Action</span> Allow</span><br></pre></td></tr></table></figure></p></li>
</ol></li>
<li><p>配置 Ubuntu apt 源</p>
<p>备份原 apt 源</p>
<p><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">sudo <span class="built_in">mv</span> /etc/apt/sources.list /etc/apt/sources.list.bak</span><br></pre></td></tr></table></figure></p>
<p>编辑 apt 源</p>
<p><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">sudo vi /etc/apt/sources.list</span><br></pre></td></tr></table></figure></p>
<p>添加国内源</p>
<ul>
<li><p>清华源</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">deb https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ &lt;Ubuntu Distribution Name&gt; main restricted universe multiverse</span><br><span class="line">deb-src https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ &lt;Ubuntu Distribution Name&gt; main restricted universe multiverse</span><br><span class="line">deb https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ &lt;Ubuntu Distribution Name&gt;-updates main restricted universe multiverse</span><br><span class="line">deb-src https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ &lt;Ubuntu Distribution Name&gt;-updates main restricted universe multiverse</span><br><span class="line">deb https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ &lt;Ubuntu Distribution Name&gt;-backports main restricted universe multiverse</span><br><span class="line">deb-src https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ &lt;Ubuntu Distribution Name&gt;-backports main restricted universe multiverse</span><br><span class="line">deb https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ &lt;Ubuntu Distribution Name&gt;-security main restricted universe multiverse</span><br><span class="line">deb-src https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ &lt;Ubuntu Distribution Name&gt;-security main restricted universe multiverse</span><br></pre></td></tr></table></figure></li>
<li><p>中科大源</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">deb https://mirrors.ustc.edu.cn/ubuntu/ &lt;Ubuntu Distribution Name&gt; main restricted universe multiverse</span><br><span class="line">deb-src https://mirrors.ustc.edu.cn/ubuntu/ &lt;Ubuntu Distribution Name&gt; main restricted universe multiverse</span><br><span class="line">deb https://mirrors.ustc.edu.cn/ubuntu/ &lt;Ubuntu Distribution Name&gt;-security main restricted universe multiverse</span><br><span class="line">deb-src https://mirrors.ustc.edu.cn/ubuntu/ &lt;Ubuntu Distribution Name&gt;-security main restricted universe multiverse</span><br><span class="line">deb https://mirrors.ustc.edu.cn/ubuntu/ &lt;Ubuntu Distribution Name&gt;-updates main restricted universe multiverse</span><br><span class="line">deb-src https://mirrors.ustc.edu.cn/ubuntu/ &lt;Ubuntu Distribution Name&gt;-updates main restricted universe multiverse</span><br><span class="line">deb https://mirrors.ustc.edu.cn/ubuntu/ &lt;Ubuntu Distribution Name&gt;-backports main restricted universe multiverse</span><br><span class="line">deb-src https://mirrors.ustc.edu.cn/ubuntu/ &lt;Ubuntu Distribution Name&gt;-backports main restricted universe multiverse</span><br></pre></td></tr></table></figure></li>
</ul>
<p>更新源</p>
<p><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">sudo apt-get update</span><br></pre></td></tr></table></figure></p>
<p>更新软件</p>
<p><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">sudo apt-get upgrade</span><br></pre></td></tr></table></figure></p></li>
</ol>
<h3 id="wsl-ubuntu-配置问题">WSL / Ubuntu 配置问题</h3>
<ol type="1">
<li><p>Ubuntu 切换阿里云源后提示缺少公钥</p>
<p>解决方法：</p>
<p>sudo apt-key adv --keyserver keyserver.ubuntu.com --recv-keys
3B4FE6ACC0B21F32</p></li>
</ol>
<h3 id="wsl-ubuntu-配置参考网页">WSL / Ubuntu 配置参考网页</h3>
<p>清华大学开源软件镜像站 - <a
href="https://mirrors.tuna.tsinghua.edu.cn/help/ubuntu/">Ubuntu
镜像使用帮助</a> 中国科技大学开源软件镜像站 - <a
href="https://mirrors.ustc.edu.cn/help/ubuntu.html">Ubuntu
源使用帮助</a> weixin_43858295 - <a
href="https://blog.csdn.net/weixin_43858295/article/details/123959824">Ubuntu
换阿里云源后更新提示： GPG error 缺少公钥解决方法</a></p>
]]></content>
      <categories>
        <category>技术</category>
        <category>Linux</category>
      </categories>
      <tags>
        <tag>WSL</tag>
        <tag>Ubuntu</tag>
      </tags>
  </entry>
  <entry>
    <title>WSL 深度学习总结</title>
    <url>/2022/08/24/wsl-shen-du-xue-xi-zong-jie/</url>
    <content><![CDATA[<h2 id="环境安装">环境安装</h2>
<ol type="1">
<li><p>下载 Anaconda for Linux</p></li>
<li><p>使用 <code>cd</code> 指令转到下载文件夹，输入如下指令安装
Anaconda</p>
<p><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">bash Anaconda3-&lt;版本&gt;-Linux-x86_64.sh</span><br></pre></td></tr></table></figure></p>
<blockquote>
<p>在 WSL 中， Windows 系统文件保存在 <code>/mnt</code> 文件夹下， C
盘对应文件夹 <code>/mnt/C</code></p>
</blockquote></li>
<li><p>配置环境变量</p>
<p>输入如下指令，编辑 profile</p>
<p><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">sudo vim /etc/profile</span><br></pre></td></tr></table></figure></p>
<p>在文件的最底端，添加如下代码</p>
<p><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line"><span class="built_in">export</span> PATH=&lt;Anaconda Adress&gt;/bin:<span class="variable">$PATH</span></span><br></pre></td></tr></table></figure></p>
<p>输入如下指令，重新加载环境变量</p>
<p><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line"><span class="built_in">source</span> /etc/profile</span><br></pre></td></tr></table></figure></p></li>
<li><p>构建 Anaconda 环境</p>
<p>输入以下指令创建新 conda 环境</p>
<p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line">conda create -n &lt;Environment Name&gt; pip python=&lt;Python Vertion&gt;</span><br></pre></td></tr></table></figure></p>
<p>输入以下指令激活 conda 环境</p>
<p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line">conda activate &lt;Environment Name&gt;</span><br></pre></td></tr></table></figure></p>
<p>使用 <code>pip install</code> 指令或 <code>conda install</code>
指令安装环境所需模块</p></li>
</ol>
<h3 id="环境安装参考网页">环境安装参考网页</h3>
<p>萝北村的枫子 - <a
href="https://blog.csdn.net/thy0000/article/details/122878599">Ubuntu
安装 Anaconda 详细步骤</a></p>
<p>文艺圈不知名刘先生 - <a
href="https://blog.csdn.net/huiruwei1020/article/details/107630269">WSL
安装 Anaconda</a></p>
<p>阿柴 - <a href="https://zhuanlan.zhihu.com/p/510556215">从零开始的
WSL 深度学习环境配置</a></p>
<p>Lyle Chen -<a href="https://zhuanlan.zhihu.com/p/453778081">基于 WSL
搭建深度学习开发环境</a></p>
<h2 id="point-net-在-wsl-上复现">Point Net 在 WSL 上复现</h2>
<p>根据作者在 GitHub 上的信息构建环境</p>
<blockquote>
<p>Installation Install TensorFlow. You may also need to install h5py.
The code has been tested with Python 2.7, TensorFlow 1.0.1, CUDA 8.0 and
cuDNN 5.1 on Ubuntu 14.04.</p>
</blockquote>
<ol type="1">
<li><p>创建 python 2.7 conda 环境</p>
<p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line">conda create -n pointnet pip python=<span class="number">2.7</span></span><br><span class="line">conda activate pointnet</span><br></pre></td></tr></table></figure></p></li>
<li><p>在 conda 环境中安装下列模块</p>
<ul>
<li>TensorFlow</li>
<li>Matplotlib</li>
<li>Pillow</li>
<li>SciPy</li>
<li>h5py</li>
<li>PIL</li>
</ul>
<p>即输入如下指令</p>
<p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line">sudo pip install h5py</span><br><span class="line">conda install h5py</span><br><span class="line">conda install tensorflow-gpu=<span class="number">1.0</span><span class="number">.1</span></span><br><span class="line">conda install matplotlib</span><br><span class="line">conda install pillow</span><br><span class="line">conda install scipy</span><br><span class="line">conda install PIL</span><br></pre></td></tr></table></figure></p></li>
<li><p>下载 <a
href="https://shapenet.cs.stanford.edu/media/modelnet40_ply_hdf5_2048.zip">modelnet40_ply_hdf5_2048</a>
解压到 <code>pointnet-master/data</code> 文件夹下</p></li>
<li><p>输入如下指令，执行 train.py</p>
<p><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">python train.py</span><br></pre></td></tr></table></figure></p>
<blockquote>
<p>train.py 中默认训练 250 epochs ， GTX 1060 训练 110 epochs 需要 40
小时，而在 80 epoch 时，训练结果已收敛，若只是复现可以略微减少训练轮次
输入 <code>python train.py --epoch &lt;epoch&gt;</code>
即可调整最大训练轮次</p>
</blockquote></li>
<li><p>train.py 运行结果片段</p>
<p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">**** EPOCH <span class="number">100</span> ****</span><br><span class="line">----<span class="number">0</span>-----</span><br><span class="line">mean loss: <span class="number">0.147063</span></span><br><span class="line">accuracy: <span class="number">0.951593</span></span><br><span class="line">----<span class="number">1</span>-----</span><br><span class="line">mean loss: <span class="number">0.169319</span></span><br><span class="line">accuracy: <span class="number">0.939941</span></span><br><span class="line">----<span class="number">2</span>-----</span><br><span class="line">mean loss: <span class="number">0.155273</span></span><br><span class="line">accuracy: <span class="number">0.940430</span></span><br><span class="line">----<span class="number">3</span>-----</span><br><span class="line">mean loss: <span class="number">0.176028</span></span><br><span class="line">accuracy: <span class="number">0.940918</span></span><br><span class="line">----<span class="number">4</span>-----</span><br><span class="line">mean loss: <span class="number">0.156226</span></span><br><span class="line">accuracy: <span class="number">0.941406</span></span><br><span class="line">----<span class="number">0</span>-----</span><br><span class="line">----<span class="number">1</span>-----</span><br><span class="line"><span class="built_in">eval</span> mean loss: <span class="number">0.564006</span></span><br><span class="line"><span class="built_in">eval</span> accuracy: <span class="number">0.874188</span></span><br><span class="line"><span class="built_in">eval</span> avg <span class="keyword">class</span> <span class="title class_">acc</span>: <span class="number">0.844451</span></span><br><span class="line">Model saved <span class="keyword">in</span> file: log/model.ckpt</span><br></pre></td></tr></table></figure></p></li>
<li><p>输入如下指令，进行测试</p>
<p><figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">python evaluate.py --visu</span><br></pre></td></tr></table></figure></p>
<p>测试结果</p>
<p><figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="built_in">eval</span> mean loss: <span class="number">0.535309</span></span><br><span class="line"><span class="built_in">eval</span> accuracy: <span class="number">0.880875</span></span><br><span class="line"><span class="built_in">eval</span> avg <span class="keyword">class</span> <span class="title class_">acc</span>: <span class="number">0.852448</span></span><br></pre></td></tr></table></figure></p></li>
<li><p>输入如下指令，在 TensorBoard 中查看训练历史</p>
<figure>
<img src="https://s2.loli.net/2022/08/19/5KaobVEOAci4W7v.png"
alt="accuracy" />
<figcaption aria-hidden="true">accuracy</figcaption>
</figure>
<figure>
<img src="https://s2.loli.net/2022/08/19/oeDEZXPg13I8Txz.png"
alt="bn_decay" />
<figcaption aria-hidden="true">bn_decay</figcaption>
</figure>
<figure>
<img src="https://s2.loli.net/2022/08/19/qEgKTBavbFm41zS.png"
alt="classify_loss" />
<figcaption aria-hidden="true">classify_loss</figcaption>
</figure>
<figure>
<img src="https://s2.loli.net/2022/08/19/sc1w7rvyXxDOH93.png"
alt="loss" />
<figcaption aria-hidden="true">loss</figcaption>
</figure>
<figure>
<img src="https://s2.loli.net/2022/08/19/ygeWncQrGSH1K3I.png"
alt="mat_loss" />
<figcaption aria-hidden="true">mat_loss</figcaption>
</figure></li>
</ol>
<h3 id="point-net-在-wsl-上复现参考网页">Point Net 在 WSL
上复现参考网页</h3>
<p>YQ8023family - <a
href="https://blog.csdn.net/qq_40234695/article/details/86223577">PointNet
复现</a></p>
<h3 id="point-net-在-wsl-上复现遇到问题">Point Net 在 WSL
上复现遇到问题</h3>
<ol type="1">
<li><p>在进行训练的时候，检查 Windows 系统任务管理器，发现 GPU
使用率基本为 0%，即没有使用 GPU 加速，且运行速度较慢</p>
<p>可能的解决方法：</p>
<ul>
<li>在检查环境的时候发现没有安装 cudnn
，怀疑可能是缺乏该模块影响了训练速度，稍后安装好后再进行尝试</li>
<li>改用服务器进行训练，服务器的显卡比本机好，运行速度较快(GTX1060)</li>
</ul>
<p>后续解决：</p>
<ul>
<li>在输入指令 <code>conda install cudnn</code> 安装 cudnn 后，再次执行
<code>train.py</code> 文件，发现已经可以调用核显(Intel® UHD Graphics
630)的 GPU ，但是独显的 GPU 使用率依旧为 0%</li>
</ul>
<p>后续可能的解决方法：</p>
<ul>
<li><del>在检查 <code>train.py</code> 文件默认参数后发现，程序默认使用
GPU 0，即本机的核显，使用指令 <code>python train.py --gpu 1</code>
执行可使用本机独显</del></li>
<li>经咨询后使用 <code>nvidia-smi</code> 指令查看显卡，发现只有独显 GTX
1060，即不存在 GPU 1</li>
</ul>
<p>最终解决方法：</p>
<ul>
<li>输入 <code>conda remove tensorflow</code> 指令移除 TensorFlow
库，输入 <code>conda install tensorflow-gpu==1.1.0</code> 安装
TensorFlow-GPU 库</li>
</ul>
<p>事后追因：</p>
<ol type="1">
<li>安装环境时输入指令 <code>nvidia-smi</code> 检查驱动，发现
<code>CUDA Version: 11.7</code> 且 <code>Driver Version: 516.94</code>
就没有去安装 cudnn</li>
<li>安装 TensorFlow 时，一开始有考虑安装 TensorFlow-GPU
版本，但是想着尽量和作者要求的版本一致，然而并不存在 TensorFlow-GPU
1.0.1 版本，最终安装了 TensorFlow 1.0.1 版本</li>
</ol>
<p>总结：</p>
<ul>
<li>安装环境时优先安装 cuda 、 cudnn ，若需要安装 TensorFlow
库时，优先安装先进版本的 TensorFlow-GPU 库</li>
</ul></li>
<li><p>WSL 上安装的 Ubuntu-22.04 LTS
因无法安装图形化界面，无法显示分类失败的图片，也没法通过输入
<code>tensorboard --logdir log</code> 后打开 <a
href="http://localhost:6006" class="uri">http://localhost:6006</a>
查看训练过程</p>
<p>可能的解决方法：</p>
<ul>
<li>将训练后整个文件夹复制到 Windows 硬盘上，在 Windows 系统下查看</li>
<li>后续实验发现，在 Ubuntu-20.04 LTS
系统上可以实现图形化界面，也可尝试在该系统上进行论文复现</li>
</ul></li>
<li><p>在执行 train.py 文件时，弹出错误信息</p>
<p><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Could not identify NUMA node of /job:localhost/replica:0/task:0/gpu:0, defaulting to 0.  Your kernel may not have been built with NUMA support.</span><br></pre></td></tr></table></figure></p>
<p>翻找 TensorFlow 源码后找到弹出错误信息的原因</p>
<blockquote>
<p>For some reason the StreamExecutor couldn't get the NUMA affinity of
the GPU. If this is not a multi-socket mobo with GPUs local to different
buses, it doesn't matter. If it is, we may run into trouble later with
data transfer operations. The trouble may manifest as slower than
expected performance, or outright failures.</p>
</blockquote>
<p>最终处理是忽略该错误信息</p></li>
</ol>
<h3 id="解决问题参考网页">解决问题参考网页</h3>
<p>小白的2015 - <a
href="https://blog.csdn.net/baixiaozhe/article/details/54598346">NUMA
support</a></p>
]]></content>
      <categories>
        <category>技术</category>
        <category>Linux</category>
      </categories>
      <tags>
        <tag>点云</tag>
        <tag>PointNet</tag>
        <tag>WSL</tag>
        <tag>Ubuntu</tag>
      </tags>
  </entry>
</search>
